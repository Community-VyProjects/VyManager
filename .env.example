# Environment Configuration for VyManager
# Copy this file to .env and customize for your deployment

# ============================================================================
# Application Environment
# ============================================================================
NODE_ENV=production

# ============================================================================
# Better Auth Configuration
# ============================================================================
# IMPORTANT: Change this secret in production!
BETTER_AUTH_SECRET=your-super-secret-key-change-in-production

# Base URL for Better Auth (where your app is accessible)
# Change this to your server's IP or domain
BETTER_AUTH_URL=http://your-server-ip:3000
NEXT_PUBLIC_APP_URL=http://your-server-ip:3000

# Trusted origins for CORS (comma-separated list)
# Include all URLs where your frontend will be accessed
# Examples:
#   Single origin: http://192.168.1.100:3000
#   Multiple origins: http://192.168.1.100:3000,http://vymanager.local:3000,https://vymanager.example.com
TRUSTED_ORIGINS=http://your-server-ip:3000,http://localhost:3000

# ============================================================================
# Notes
# ============================================================================
# - Change BETTER_AUTH_SECRET to a long, random string
# - Update BETTER_AUTH_URL and NEXT_PUBLIC_APP_URL to match your deployment
# - Add all origins where users will access the app to TRUSTED_ORIGINS
# - For production, consider using HTTPS URLs
